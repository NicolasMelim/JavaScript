const celular = ['Iphone', 'Xiaomi' , 'Samsung']

for(let i of celular){
    console.log(i)
}

// Usamos o OFF para pegar o valor de uma array, sem o OFF pegaremos o index, como exemplo = 0, 1 , 2!!!!!!